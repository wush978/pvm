# R Package Version Manager

## Feature

- Record the version of the package via `installed.packages` to a JSON file
- Install the package of the specific version from CRAN(source) or MRAN(binary)
  according to the JSON file

## Usage

